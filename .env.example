# The browser-only LDB SafeHub preview does not require environment variables.
#
# Production and UAT must use separate hostnames so browser localStorage is
# isolated by origin. This source snapshot does not include real credentials,
# transaction records, attachments, or history.
